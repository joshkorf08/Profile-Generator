const Employee = require("../lib/employee.js");
const Intern =require("../lib/intern")


test("Will set school with constructor", () => {
    const testValue = "MSU";
    const E = new Intern("");
    expect();
});

test("will get school through getSchool()", () => {
    const testValue = "MSU";
    const E = new Intern("");
    expect(typeof(E)).toBe("object");
}) 



